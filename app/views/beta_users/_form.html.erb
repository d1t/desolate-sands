<!DOCTYPE html>
<div class="brand_logo">
 <a href="/"><img class="masthead-brand center-block" src="/cropgrnblkexp2.png" style="max-height: 100px;">
</div></a>

<%= form_for(@beta_user) do |f| %>
  <% if @beta_user.errors.any? %>
    <div class="error_msg" style="margin-right:auto; margin-left:auto;">
      <div id="error_explanation">
        <h2><%= pluralize(@beta_user.errors.count, "error") %> prohibited your form being saved:</h2>

        <ul>
        <% @beta_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

    <div class="form-group text-center">
      <h4 class="header"><strong>Cubey Private Beta Invite</strong></h4>
    </div>
    <div class="form-group text-center">Please complete the form to sign up for the Private Beta and a member of the team will get back to you ASAP
    </div>

    <div class="form text-center">
      <div class="form-group">
        <%= f.text_field :first_name, placeholder: "First Name" %>
      </div>
      <div class="form-group">
        <%= f.text_field :last_name, placeholder: "Last Name" %>
      </div>
      <div class="form-group">
        <%= f.text_field :email, placeholder: "Email (eg. john@example.com)" %>
      </div>
      <div class="form-group">
        <%= f.label :role, class: "role_label" %><br>
        <%=  f.select :role, BetaUser::ROLE_OPTIONS, prompt: "Please select" %>
      </div>
      <div class="form-group actions">
        <%= f.submit "Sign up", class: "btn" %>
      </div>
    </div>
<% end %>